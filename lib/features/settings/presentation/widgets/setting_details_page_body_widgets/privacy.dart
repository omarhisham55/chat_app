import 'package:flutter/material.dart';

class PrivacyBodyContent extends StatelessWidget {
  const PrivacyBodyContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}